import{Shell}from"@/components/shell";import{getWorkspace}from"@/lib/workspace";
export default async function Page(){const w=await getWorkspace();const{data}=await w.supabase.from("activities").select("*").eq("company_id",w.companyId).order("created_at",{ascending:false}).limit(100);return <Shell company={w.companyName}><h1 className="text-3xl font-black">Activity timeline</h1><p className="text-slate-600">A chronological record of business changes.</p><div className="card mt-6"><div className="grid gap-5">{data?.map(x=><div key={x.id} className="border-l-2 border-slate-300 pl-4"><p className="font-black">{x.title}</p><p className="text-sm text-slate-600">{x.entity_type} · {new Date(x.created_at).toLocaleString()}</p></div>)}{!data?.length&&<p>No activity yet. New changes will appear after the V3 migration is installed.</p>}</div></div></Shell>}
