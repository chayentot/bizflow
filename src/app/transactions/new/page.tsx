import Link from "next/link";import {createTransaction} from "@/app/actions";import {Shell} from "@/components/shell";import {FormButton} from "@/components/form-button";import {getWorkspace} from "@/lib/workspace";
export default async function NewTransaction({searchParams}:{searchParams:Promise<{error?:string}>}){const w=await getWorkspace();const q=await searchParams;const today=new Date().toISOString().slice(0,10);return <Shell company={w.companyName}><div className="max-w-2xl"><Link className="text-sm font-bold text-slate-600" href="/transactions">← Back to finances</Link><h1 className="mt-4 text-3xl font-black">Add transaction</h1>{q.error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-red-700">{q.error}</p>}<form action={createTransaction} className="card mt-6 space-y-4"><label><span className="label">Type</span><select name="type"><option value="income">Income</option><option value="expense">Expense</option></select></label><label><span className="label">Category</span><input name="category" required/></label><label><span className="label">Amount</span><input name="amount" type="number" min="0.01" step="0.01" required/></label><label><span className="label">Date</span><input name="transaction_date" type="date" defaultValue={today} required/></label><label><span className="label">Description</span><textarea name="description" rows={3}/></label><div className="flex gap-3"><FormButton pendingText="Adding transaction...">Add transaction</FormButton><Link className="btn-secondary rounded-lg px-4 py-2 font-bold" href="/transactions">Cancel</Link></div></form></div></Shell>}
