import Link from "next/link";import {Shell} from "@/components/shell";import {EmptyState,PageHeader} from "@/components/page-header";import {getWorkspace} from "@/lib/workspace";
const money=(v:number|string)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(Number(v));
export default async function Invoices({searchParams}:{searchParams:Promise<{error?:string;q?:string}>}){const w=await getWorkspace();const q=await searchParams;const term=(q.q??"").trim();let query=w.supabase.from("invoices").select("id,invoice_number,status,issue_date,due_date,total,customers(name,company_name)").eq("company_id",w.companyId).order("created_at",{ascending:false});if(term)query=query.ilike("invoice_number",`%${term}%`);const {data:invoices}=await query;const today=new Date().toISOString().slice(0,10);return <Shell company={w.companyName}><PageHeader title="Invoices" description="Review invoices and payment status." actionHref="/invoices/new" actionLabel="Create invoice"/>{q.error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-red-700">{q.error}</p>}<form className="mt-6 flex max-w-xl gap-2"><input name="q" defaultValue={term} placeholder="Search invoice number"/><button className="btn-secondary rounded-lg px-4 font-bold">Search</button></form><section className="card mt-6 overflow-x-auto p-0">{invoices?.length?<table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-4">Invoice</th><th className="p-4">Customer</th><th className="p-4">Status</th><th className="p-4">Issue date</th><th className="p-4">Due date</th><th className="p-4 text-right">Total</th></tr></thead><tbody>{invoices.map(i=>{const c=i.customers as unknown as {name:string;company_name:string|null}|null;const overdue=i.status!=="paid"&&i.due_date<today;return <tr key={i.id} className="border-b last:border-0 hover:bg-slate-50"><td className="p-4 font-bold"><Link className="text-blue-700 hover:underline" href={`/invoices/${i.id}`}>{i.invoice_number}</Link></td><td className="p-4">{c?.company_name||c?.name||"Customer"}</td><td className="p-4"><span className={`rounded-full px-2 py-1 text-xs font-bold uppercase ${overdue?"bg-red-100 text-red-700":i.status==="paid"?"bg-emerald-100 text-emerald-700":"bg-slate-100"}`}>{overdue?"overdue":i.status}</span></td><td className="p-4">{i.issue_date}</td><td className="p-4">{i.due_date}</td><td className="p-4 text-right font-black">{money(i.total)}</td></tr>})}</tbody></table>:<div className="p-6"><EmptyState title="No invoices found" description="Create your first invoice for a customer." href="/invoices/new" label="Create invoice"/></div>}</section></Shell>}
