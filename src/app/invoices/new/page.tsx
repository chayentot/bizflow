import Link from "next/link";import {createInvoice} from "@/app/actions";import {Shell} from "@/components/shell";import {FormButton} from "@/components/form-button";import {getWorkspace} from "@/lib/workspace";
const money=(v:number|string)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(Number(v));
export default async function NewInvoice({searchParams}:{searchParams:Promise<{error?:string}>}){const w=await getWorkspace();const q=await searchParams;const [{data:customers},{data:products}]=await Promise.all([w.supabase.from("customers").select("id,name,company_name").eq("company_id",w.companyId).order("name"),w.supabase.from("products").select("id,name,sku,selling_price,quantity").eq("company_id",w.companyId).gt("quantity",0).order("name")]);const today=new Date().toISOString().slice(0,10);const due=new Date(Date.now()+14*86400000).toISOString().slice(0,10);return <Shell company={w.companyName}><div className="max-w-3xl"><Link className="text-sm font-bold text-slate-600" href="/invoices">← Back to invoices</Link><h1 className="mt-4 text-3xl font-black">Create invoice</h1>{q.error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-red-700">{q.error}</p>}<form action={createInvoice} className="card mt-6 grid gap-4 sm:grid-cols-2">{!customers?.length&&<p className="rounded-lg bg-amber-50 p-3 text-sm text-amber-800 sm:col-span-2">Add a customer before creating an invoice.</p>}<label className="sm:col-span-2"><span className="label">Customer</span><select name="customer_id" required disabled={!customers?.length}><option value="">Select customer</option>{customers?.map(c=><option key={c.id} value={c.id}>{c.name}{c.company_name?` — ${c.company_name}`:""}</option>)}</select></label><label><span className="label">Issue date</span><input name="issue_date" type="date" defaultValue={today} required/></label><label><span className="label">Due date</span><input name="due_date" type="date" defaultValue={due} required/></label><label className="sm:col-span-2"><span className="label">Status</span><select name="status"><option value="draft">Draft</option><option value="sent">Sent</option><option value="paid">Paid</option></select></label><label className="sm:col-span-2"><span className="label">Inventory product (optional)</span><select name="product_id"><option value="">Manual service or item</option>{products?.map(p=><option key={p.id} value={p.id}>{p.name} — {p.sku} ({p.quantity} available, {money(p.selling_price)})</option>)}</select></label><label className="sm:col-span-2"><span className="label">Description</span><input name="description" placeholder="Required for manual items"/></label><label><span className="label">Quantity</span><input name="quantity" type="number" min="1" defaultValue="1" required/></label><label><span className="label">Unit price</span><input name="unit_price" type="number" min="0" step="0.01" required/></label><label><span className="label">Tax %</span><input name="tax_rate" type="number" min="0" step="0.01" defaultValue="0"/></label><label><span className="label">Discount</span><input name="discount" type="number" min="0" step="0.01" defaultValue="0"/></label><label className="sm:col-span-2"><span className="label">Notes</span><textarea name="notes" rows={3}/></label><div className="flex gap-3 sm:col-span-2"><FormButton disabled={!customers?.length} pendingText="Creating invoice...">Create invoice</FormButton><Link className="btn-secondary rounded-lg px-4 py-2 font-bold" href="/invoices">Cancel</Link></div></form></div></Shell>}
