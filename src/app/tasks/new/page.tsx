import Link from "next/link";import {createTask} from "@/app/actions";import {Shell} from "@/components/shell";import {FormButton} from "@/components/form-button";import {getWorkspace} from "@/lib/workspace";
export default async function NewTask(){const w=await getWorkspace();return <Shell company={w.companyName}><div className="max-w-2xl"><Link className="text-sm font-bold text-slate-600" href="/tasks">← Back to tasks</Link><h1 className="mt-4 text-3xl font-black">Add task</h1><form action={createTask} className="card mt-6 space-y-4"><label><span className="label">Title</span><input name="title" required/></label><label><span className="label">Description</span><textarea name="description" rows={4}/></label><div className="grid grid-cols-2 gap-3"><label><span className="label">Status</span><select name="status"><option value="todo">To do</option><option value="in_progress">In progress</option><option value="review">Review</option></select></label><label><span className="label">Priority</span><select name="priority"><option>low</option><option>medium</option><option>high</option></select></label></div><label><span className="label">Due date</span><input type="date" name="due_date"/></label><div className="flex gap-3"><FormButton pendingText="Adding task...">Add task</FormButton><Link className="btn-secondary rounded-lg px-4 py-2 font-bold" href="/tasks">Cancel</Link></div></form></div></Shell>}
