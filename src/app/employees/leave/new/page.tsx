import Link from "next/link";import {createLeaveRequest} from "@/app/actions";import {Shell} from "@/components/shell";import {FormButton} from "@/components/form-button";import {getWorkspace} from "@/lib/workspace";
export default async function NewLeave({searchParams}:{searchParams:Promise<{error?:string}>}){const w=await getWorkspace();const q=await searchParams;const {data:employees}=await w.supabase.from("employees").select("id,first_name,last_name").eq("company_id",w.companyId).order("first_name");const today=new Date().toISOString().slice(0,10);return <Shell company={w.companyName}><div className="max-w-2xl"><Link className="text-sm font-bold text-slate-600" href="/employees">← Back to employees</Link><h1 className="mt-4 text-3xl font-black">New leave request</h1>{q.error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-red-700">{q.error}</p>}<form action={createLeaveRequest} className="card mt-6 grid gap-4 sm:grid-cols-2"><label className="sm:col-span-2"><span className="label">Employee</span><select name="employee_id" required><option value="">Select employee</option>{employees?.map(e=><option key={e.id} value={e.id}>{e.first_name} {e.last_name}</option>)}</select></label><label className="sm:col-span-2"><span className="label">Leave type</span><select name="leave_type"><option value="vacation">Vacation</option><option value="sick">Sick</option><option value="personal">Personal</option><option value="parental">Parental</option><option value="other">Other</option></select></label><label><span className="label">Start date</span><input type="date" name="start_date" defaultValue={today} required/></label><label><span className="label">End date</span><input type="date" name="end_date" defaultValue={today} required/></label><label className="sm:col-span-2"><span className="label">Reason</span><textarea name="reason" rows={3}/></label><div className="flex gap-3 sm:col-span-2"><FormButton disabled={!employees?.length} pendingText="Submitting request...">Submit request</FormButton><Link className="btn-secondary rounded-lg px-4 py-2 font-bold" href="/employees">Cancel</Link></div></form></div></Shell>}
