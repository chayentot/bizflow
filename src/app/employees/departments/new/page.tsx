import Link from "next/link";import {createDepartment} from "@/app/actions";import {Shell} from "@/components/shell";import {FormButton} from "@/components/form-button";import {getWorkspace} from "@/lib/workspace";
export default async function NewDepartment({searchParams}:{searchParams:Promise<{error?:string}>}){const w=await getWorkspace();const q=await searchParams;return <Shell company={w.companyName}><div className="max-w-2xl"><Link className="text-sm font-bold text-slate-600" href="/employees">← Back to employees</Link><h1 className="mt-4 text-3xl font-black">Add department</h1>{q.error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-red-700">{q.error}</p>}<form action={createDepartment} className="card mt-6 space-y-4"><label><span className="label">Department name</span><input name="name" required/></label><label><span className="label">Description</span><textarea name="description" rows={3}/></label><div className="flex gap-3"><FormButton pendingText="Creating department...">Create department</FormButton><Link className="btn-secondary rounded-lg px-4 py-2 font-bold" href="/employees">Cancel</Link></div></form></div></Shell>}
