import Link from 'next/link'; import { signUp } from '@/app/actions';
export default async function Signup({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="min-h-screen grid place-items-center p-5"><form action={signUp} className="card w-full max-w-md space-y-4"><h1 className="text-3xl font-black">Create your account</h1>{q.error&&<p className="rounded-lg bg-red-50 p-3 text-red-700">{q.error}</p>}<label><span className="label">Full name</span><input name="full_name" required/></label><label><span className="label">Email</span><input name="email" type="email" required/></label><label><span className="label">Password</span><input name="password" type="password" minLength={6} required/></label><button className="btn w-full">Register</button><p className="text-sm">Already registered? <Link className="font-bold underline" href="/login">Sign in</Link></p></form></main>}
