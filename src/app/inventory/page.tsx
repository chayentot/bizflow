import Link from "next/link";import {adjustStock,deleteProduct} from "@/app/actions";import {Shell} from "@/components/shell";import {EmptyState,PageHeader} from "@/components/page-header";import {getWorkspace} from "@/lib/workspace";
const money=(v:number|string)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(Number(v));
export default async function Inventory({searchParams}:{searchParams:Promise<{error?:string;q?:string}>}){const w=await getWorkspace();const q=await searchParams;const term=(q.q??"").trim();let query=w.supabase.from("products").select("id,name,sku,barcode,cost,selling_price,quantity,low_stock_threshold").eq("company_id",w.companyId).order("name");if(term)query=query.or(`name.ilike.%${term}%,sku.ilike.%${term}%`);const {data:products}=await query;const list=products??[];const totalUnits=list.reduce((s,p)=>s+Number(p.quantity),0);const inventoryValue=list.reduce((s,p)=>s+Number(p.quantity)*Number(p.cost),0);const lowStock=list.filter(p=>Number(p.quantity)<=Number(p.low_stock_threshold));return <Shell company={w.companyName}><PageHeader title="Inventory" description="View products, stock levels, and pricing." actionHref="/inventory/new" actionLabel="Add product"/>{q.error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-red-700">{q.error}</p>}<div className="mt-6 grid gap-4 sm:grid-cols-3"><div className="card"><p className="text-sm text-slate-500">Products</p><p className="mt-2 text-3xl font-black">{list.length}</p></div><div className="card"><p className="text-sm text-slate-500">Units in stock</p><p className="mt-2 text-3xl font-black">{totalUnits}</p></div><div className="card"><p className="text-sm text-slate-500">Inventory cost</p><p className="mt-2 text-3xl font-black">{money(inventoryValue)}</p></div></div>{lowStock.length>0&&<section className="mt-6 rounded-xl border border-amber-200 bg-amber-50 p-4"><h2 className="font-black text-amber-900">Low-stock alert</h2><p className="mt-1 text-sm text-amber-800">{lowStock.map(p=>`${p.name} (${p.quantity})`).join(", ")}</p></section>}<form className="mt-6 flex max-w-xl gap-2"><input name="q" defaultValue={term} placeholder="Search product or SKU"/><button className="btn-secondary rounded-lg px-4 font-bold">Search</button>{term&&<Link className="rounded-lg px-3 py-2 text-sm font-bold" href="/inventory">Clear</Link>}</form><section className="card mt-6 overflow-x-auto p-0">{list.length?<table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-4">Product</th><th className="p-4">SKU</th><th className="p-4 text-right">Cost</th><th className="p-4 text-right">Price</th><th className="p-4 text-right">Stock</th><th className="p-4">Adjust stock</th><th className="p-4 text-right">Action</th></tr></thead><tbody>{list.map(p=>{const low=Number(p.quantity)<=Number(p.low_stock_threshold);return <tr key={p.id} className="border-b last:border-0"><td className="p-4 font-bold">{p.name}{p.barcode&&<span className="block text-xs font-normal text-slate-500">{p.barcode}</span>}</td><td className="p-4">{p.sku}</td><td className="p-4 text-right">{money(p.cost)}</td><td className="p-4 text-right">{money(p.selling_price)}</td><td className="p-4 text-right"><span className={low?"rounded-full bg-amber-100 px-2 py-1 font-bold text-amber-800":"font-bold"}>{p.quantity}</span></td><td className="p-4"><form action={adjustStock} className="flex gap-2"><input type="hidden" name="id" value={p.id}/><input className="w-24" name="quantity_change" type="number" step="1" placeholder="+ / -" required/><input className="w-32" name="note" placeholder="Reason"/><button className="rounded-lg border px-3 py-2 text-sm font-bold">Save</button></form></td><td className="p-4 text-right"><form action={deleteProduct}><input type="hidden" name="id" value={p.id}/><button className="text-sm font-bold text-red-600">Delete</button></form></td></tr>})}</tbody></table>:<div className="p-6"><EmptyState title="No products found" description="Add your first product to begin tracking stock." href="/inventory/new" label="Add product"/></div>}</section></Shell>}
