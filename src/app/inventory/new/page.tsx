import Link from "next/link";import {createProduct} from "@/app/actions";import {Shell} from "@/components/shell";import {FormButton} from "@/components/form-button";import {getWorkspace} from "@/lib/workspace";
export default async function NewProduct({searchParams}:{searchParams:Promise<{error?:string}>}){const w=await getWorkspace();const q=await searchParams;return <Shell company={w.companyName}><div className="max-w-2xl"><Link className="text-sm font-bold text-slate-600" href="/inventory">← Back to inventory</Link><h1 className="mt-4 text-3xl font-black">Add product</h1>{q.error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-red-700">{q.error}</p>}<form action={createProduct} className="card mt-6 grid gap-4 sm:grid-cols-2"><label className="sm:col-span-2"><span className="label">Product name</span><input name="name" required/></label><label><span className="label">SKU</span><input name="sku" required/></label><label><span className="label">Barcode</span><input name="barcode"/></label><label className="sm:col-span-2"><span className="label">Description</span><textarea name="description" rows={3}/></label><label><span className="label">Cost</span><input name="cost" type="number" min="0" step="0.01" defaultValue="0" required/></label><label><span className="label">Selling price</span><input name="selling_price" type="number" min="0" step="0.01" defaultValue="0" required/></label><label><span className="label">Starting quantity</span><input name="quantity" type="number" min="0" defaultValue="0" required/></label><label><span className="label">Low-stock level</span><input name="low_stock_threshold" type="number" min="0" defaultValue="5" required/></label><div className="flex gap-3 sm:col-span-2"><FormButton pendingText="Adding product...">Add product</FormButton><Link className="btn-secondary rounded-lg px-4 py-2 font-bold" href="/inventory">Cancel</Link></div></form></div></Shell>}
