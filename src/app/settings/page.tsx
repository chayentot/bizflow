import{Shell}from"@/components/shell";import{getWorkspace}from"@/lib/workspace";import{updateCompanySettings}from"@/app/actions";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const w=await getWorkspace();const q=await searchParams;const{data:s}=await w.supabase.from("company_settings").select("*").eq("company_id",w.companyId).maybeSingle();return <Shell company={w.companyName}><h1 className="text-3xl font-black">Company settings</h1><p className="text-slate-600">Branding and business defaults used across BizFlow.</p>{q.message&&<p className="mt-4 rounded-lg bg-emerald-50 p-3 text-emerald-700">{q.message}</p>}{q.error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-red-700">{q.error}</p>}<form action={updateCompanySettings} className="card mt-6 grid max-w-3xl gap-5 sm:grid-cols-2"><label><span className="label">Currency</span><select name="currency" defaultValue={s?.currency??"USD"}><option>USD</option><option>PHP</option><option>EUR</option><option>GBP</option><option>JPY</option></select></label><label><span className="label">Default tax rate (%)</span><input name="tax_rate" type="number" step="0.01" defaultValue={s?.tax_rate??0}/></label><label><span className="label">Time zone</span><input name="timezone" defaultValue={s?.timezone??"UTC"}/></label><label><span className="label">Business email</span><input name="email" type="email" defaultValue={s?.email??""}/></label><label><span className="label">Phone</span><input name="phone" defaultValue={s?.phone??""}/></label><label><span className="label">Website</span><input name="website" defaultValue={s?.website??""}/></label><label className="sm:col-span-2"><span className="label">Address</span><textarea name="address" defaultValue={s?.address??""}/></label><div className="sm:col-span-2"><button className="btn">Save settings</button></div></form></Shell>}
